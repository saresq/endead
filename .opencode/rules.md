# Operational Rules for this Project

1.  **ALWAYS USE `edit` FOR EXISTING FILES**: When modifying any file that already exists in the project, you must use the `edit` tool. Do not use the `write` tool to overwrite existing files, as this destroys their previous content and context. The `write` tool is reserved strictly for creating new files that do not yet exist.
